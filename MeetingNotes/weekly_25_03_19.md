# Preparation

## Recap

- Implemented top level unit tests
- Graceful handling of unsupported look-arounds in other engines
  - If the regex engines return build errors, the meta-engine will work properly
- Turn off prefilters for regexes with lookarounds

# Meeting notes

Further steps:

- Prepare a draft for the pull request
- Fuzzing
- Benchmarking (via rebar)
- Check if any engine can implement lookarounds
