# Preparation

## Recap

- HIR already has variants for simple lookarounds
- implemented a variant for complex lookarounds by wrapping the simple variant
  in a new enum
- Wrote down implementaion plan

## Questions / Discussion points

# Meeting notes

- rename look/simplelook
- add new variant directly to `HirKind`
- change `push_group` to `push_paren` and perform the lookaround distinction there

- Focus on `regex` and postpone further investigation of `fancy-regex` for now
