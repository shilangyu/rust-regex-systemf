# Preparation

## Recap

- Captureless lookbehinds implemented, but not tested yet
- Will need to investigate how to handle the added NFA instructions in other
  engines (meta engine must either realize a failure or skip to the PikeVM)
- Discussion with Clément:
  - There might be benchmarks for V8 that we can reuse
  - We should make sure to compare struct sizes before and after our changes
  - Might be possible to use prefilters even with lookarounds in case where
    a prefilter can be extracted from the lookaround, based on the 'earliest'
    prefilter match. Otherwise, for regexes with lookarounds of bounded length,
    can use the prefilter and start the bounded length before the prefilter match.

## Questions / Discussion points

- Can we use the same compilation strategy for the backtracking engine?

# Meeting notes

Further steps:
- meta engine decision
- graceful handling of unsupported operations in other engines
- Top level unit tests
- Fuzzing
- Benchmarking (via rebar)
